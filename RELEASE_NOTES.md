# Release Notes

## v0.7.0 - 2026-05-21

### 版本定位

v0.7.0 将内容工厂从 v1 主链推进到更完整的 v2 工作台：围绕品牌 / IP 知识库、输入源、场景提示词、视频 Prompt、混剪包和 SOP 工作流，形成从资料沉淀到多模态生产交付的桌面端闭环。

### 新增 / 调整

- 新增品牌 / 产品知识库与 IP 知识库工作台，支持从资料抽取结构化知识、生成场景库，并把引用带入 Prompt Pack 和后续生成链路。
- 新增输入源、反推参考、提示词工作台、场景提示词、视频 Prompt、视频导入、混剪包导出、绿幕混剪等 v2 模块入口和业务页面。
- 新增 SOP / 工作流引擎相关存储与执行链路，支持围绕文章、图片、视频和资产审核记录可追溯流程产物。
- 图片工作台继续收敛为桌面端生产界面，补强技能选择、提示词引用、批量生成、结果详情和历史复用体验。
- skills 管理增强为内置技能 / 用户安装技能的工作台，并新增 `.skill` 包安装入口、文件读取和拖拽导入能力。
- 设置、侧边栏、模块弹窗和样式分层继续拆分，降低 `App.tsx` 和单一样式文件的职责。
- Electron smoke 与 Playwright E2E 默认静默运行，保留 headed 调试入口，减少本地验证时反复弹窗。
- 打包配置补强内部预览发布所需资源和构建边界，继续保持 macOS unsigned 预览包策略。

### 工程与验证

- 同步扩展 `src/shared/types.ts`、主进程 IPC、preload bridge 和前端调用方，保证跨进程协议一致。
- 新增多组主进程 store / service，覆盖品牌知识、IP 知识、输入源、提示词草稿、工作流、混剪包、资产审核和反推参考等持久化边界。
- 扩展功能测试和 Electron E2E，覆盖 v2 业务链路、真实 Provider mock、blocked 分支、skills 安装和静默 GUI smoke。
- 更新版本号到 `0.7.0`，同步 `package.json` 和 `package-lock.json`。

### 验证

- `npm run verify:local` 通过，覆盖 `typecheck`、`build`、27 个功能测试、Electron smoke 和 15 个 E2E。

### 明确不包含

- 未配置真实文字 / 图片 / 视频生成服务时仍返回 `blocked`，不伪造生成成功。
- 正式签名、notarization、云端协作、多租户和计费能力仍为后续任务。
