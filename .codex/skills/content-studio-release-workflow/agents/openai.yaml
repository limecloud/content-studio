interface:
  display_name: "Content Studio 发版流程"
  short_description: "执行 Content Studio 版本号、发布说明、验证门禁与 tag 推送。"
  default_prompt: "使用 $content-studio-release-workflow 准备一次 Content Studio 发版，更新版本和发布说明，运行必要门禁，并处理 commit / tag / push 确认。"

policy:
  allow_implicit_invocation: true
